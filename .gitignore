# ---------------------------------------------------------------- Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/

# ---------------------------------------------------------- environments
.venv/
venv/
env/
.env
.env.*
!.env.example

# ------------------------------------------------------------ notebooks
.ipynb_checkpoints/

# ---------------------------------------------------------- IDE / OS
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
desktop.ini

# ------------------------------------------------------ real data (licence)
# The observation CSVs are derived from external releases with their own terms
# and are produced by notebooks/03. Only the three files the app reads at
# runtime are committed (with attribution in DATA.md), so a deployment built
# from this repository has its evidence. Everything else stays local.
data/real/*.csv
!data/real/flaring_by_field.csv
!data/real/flaring_by_country.csv
!data/real/methane_by_field_s5p.csv
notebooks/executed/results/*.csv

# -------------------------------------------------- local, never committed
# local model export from notebook 01 (the model is published on the Hub instead)
greentruth-claim-detector/
# raw World Bank downloads
viirs_raw/
*.log
*.tmp
screenshots-dev/
playwright-report/
test-results/

# Local design reference only (Notika admin template by Colorlib, MIT).
# The interface follows its design language; the one asset used from it, the
# notika-icon font, is vendored in web/fonts/ with attribution.
notika-master/

# data/geo/world_110m.json IS committed (public domain, small).
